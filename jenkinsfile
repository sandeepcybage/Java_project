  properties([parameters([choice(choices: ['ONE', 'TWO', 'THREE'], description: '', name: 'Service')])])
  pipeline {
  agent any
   stages {
      stage('checkout master') {
         steps {
           sh label: '', 
           script: 
           '''
           echo ${params.Service}
           '''
         }
      }
   }
}
