pipeline {
    agent any
    stages {
        stage('Set Up') {
            steps {
                sh 'date'
            }
        }
        stage('Build') { 
            steps {
                sh 'mvn clean package' 
            }
       }
}
