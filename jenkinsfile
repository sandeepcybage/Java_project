pipeline {
    agent any
    stages {
         stage {
            steps {
               sh label: '', 
                  script: 
                  '''
                  git pull origin master
                  '''
                  } 
               }
         stage('Set Up') {
            steps {
                sh 'mvn clean install' 
            }
        }
    }
}
