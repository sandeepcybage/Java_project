pipeline {
    agent any
    stages {
        stage('Set Up') {
            steps {
                sh 'date'
                sh 'echo GIT_COMMIT %GIT_COMMIT%'
                sh 'echo GIT_BRANCH %GIT_BRANCH%'
            }
        }
    }
}
